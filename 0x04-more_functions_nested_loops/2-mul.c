#include "main.h"

/**
 * mul - program that that multiplies two integers.
 * @a: the variable a
 * @b: the variable b
 * Return: returns the value of a * b
*/
int mul(int a, int b)
{
	return (a * b);
}
