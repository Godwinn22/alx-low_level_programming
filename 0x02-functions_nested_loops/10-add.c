#include "main.h"

/**
 * add - Entry point
 * @a :parameter for the value a
 * @b : parameter for the value b
 *
 * Return: Always 0 (success)
*/
int add(int a, int b)
{
	int r;

	r = (a + b);
	return (r);
}
